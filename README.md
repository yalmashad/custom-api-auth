# Custom API Authentication Demo

This repository contains:

- A small API service that uses a custom header for authentication
- A couple of API endpoints that use standard JWT bearer authentication
- A standalone Python traffic generator that sends mixed successful and failed requests

## Authentication

Custom-auth endpoints expect this non-standard header:

```text
X-Demo-Authenticated: f5-poc-secret
```

The JWT endpoints expect this standard header:

```text
Authorization: Bearer <jwt>
```

Behavior:

- Correct auth value: `200`
- Missing auth value: `401`
- Wrong auth value: `401`

## API Endpoints

Custom-authenticated endpoints:

```text
GET    /api/v1/customers?status=active&region=emea
GET    /api/v1/customers/{customerId}
GET    /api/v1/customers/{customerId}/orders
GET    /api/v1/orders/{orderId}
POST   /api/v1/orders
PUT    /api/v1/orders/{orderId}
GET    /api/v1/invoices?customerId={customerId}&status=open
POST   /api/v1/payments
DELETE /api/v1/sessions/{sessionId}
```

JWT-authenticated endpoints:

```text
GET /api/v1/reports/sales-summary?period=last-30-days
GET /api/v1/security/audit-events?severity=high
```

Health endpoint:

```text
GET /health
```

## OpenAPI

Upload this file to an API discovery engine:

```text
openapi.json
```

The OpenAPI document includes both authentication schemes:

- `CustomHeaderAuth`: `X-Demo-Authenticated`
- `JwtBearerAuth`: `Authorization: Bearer <jwt>`

## Run The API

```bash
docker compose up --build
```

The API listens on:

```text
http://localhost:8080
```

## Test The API

Custom auth success:

```bash
curl -i -H "X-Demo-Authenticated: f5-poc-secret" \
  http://localhost:8080/api/v1/customers/cust-1001
```

Custom auth missing:

```bash
curl -i http://localhost:8080/api/v1/customers/cust-1001
```

Custom auth wrong value:

```bash
curl -i -H "X-Demo-Authenticated: wrong-value" \
  http://localhost:8080/api/v1/customers/cust-1001
```

JWT auth is generated by the traffic generator. To print a valid demo JWT:

```bash
python3 -c "from traffic_generator import create_jwt; print(create_jwt())"
```

You can also use this static test JWT with the default demo configuration. It expires on `2030-01-01 00:00:00 UTC`.

```text
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJjdXN0b20tYXBpLWF1dGgtZGVtbyIsInN1YiI6ImdpdGh1Yi10ZXN0LXRva2VuIiwiYXVkIjoiY3VzdG9tLWFwaS1hdXRoLWRlbW8iLCJzY29wZSI6InJlcG9ydHM6cmVhZCBhdWRpdDpyZWFkIiwiaWF0IjoxNzgyNzQwMDAwLCJleHAiOjE4OTM0NTYwMDB9.oFOD2TGgB2iBBrYnZyVkcB16ECZ7rPHR44r1LjNoz-s
```

JWT auth success:

```bash
curl -i -H "Authorization: Bearer <jwt>" \
  http://localhost:8080/api/v1/reports/sales-summary
```

JWT auth missing:

```bash
curl -i http://localhost:8080/api/v1/reports/sales-summary
```

## Traffic Generator

Run the standalone traffic generator:

```bash
python3 traffic_generator.py \
  --base-url http://localhost:8080 \
  --total-requests 100 \
  --success-rate 0.70 \
  --interval-seconds 0.2
```

Run it against another API endpoint:

```bash
python3 traffic_generator.py \
  --base-url https://<api-hostname> \
  --total-requests 1000 \
  --success-rate 0.70 \
  --interval-seconds 0.5
```

Use `--total-requests 0` to run continuously.

The traffic generator sends both `custom-header` and `jwt` traffic. Each log line includes the auth scheme:

```json
{"authScheme": "jwt", "status": 200}
{"authScheme": "custom-header", "status": 401}
```
