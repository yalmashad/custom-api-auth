services:
  demo-api:
    build:
      context: .
      dockerfile: Dockerfile.api
    environment:
      API_PORT: "8080"
      AUTH_HEADER_NAME: "X-Demo-Authenticated"
      AUTH_HEADER_VALUE: "f5-poc-secret"
    ports:
      - "8080:8080"
